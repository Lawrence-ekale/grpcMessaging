server:
  host: localhost
  port: 9000
grpc:
  server:
    port: 9001
spring:
  application:
    name: MESSAGE-MICROSERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/messages
    username: lawispringboot
    password: Lawrence
  jpa:
    hibernate:
      ddl-auto: update
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
